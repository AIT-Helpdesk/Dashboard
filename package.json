{
  "name": "ambient-it-dashboard",
  "version": "1.0.0",
  "private": true,
  "description": "Multi-page internal dashboard backed by the Autotask API",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "npm start --workspace=@dashboard/shell"
  },
  "engines": {
    "node": ">=23.8.0"
  }
}
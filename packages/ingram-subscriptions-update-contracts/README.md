# @dashboard/ingram-subscriptions-update-contracts

No code of its own -- a second sidebar entry for `packages/ingram-subscriptions`. See `packages/ingram-orders-update-contracts/README.md` for the full explanation of this pattern (why it exists, exactly how the relative `client`/`server` paths resolve, and what is/isn't actually shared between the two entries) -- this package follows it identically, just pointed at `ingram-subscriptions` instead.
